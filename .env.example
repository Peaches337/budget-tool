DATABASE_URL=postgresql://skint:changeme@localhost:5432/skint
SESSION_SECRET=change-this-to-a-random-64-char-string
PORT=3000
BACKUP_PATH=/var/backups/skint
# Set to true only if serving over HTTPS — browsers drop Secure cookies on plain HTTP
SECURE_COOKIE=false
# Optional: full path to pg_dump/psql if not in PATH (e.g. Windows dev)
# On Linux after: apt install postgresql-client  — leave blank
PG_DUMP_PATH=
PSQL_PATH=
# Bank integration — generate with: node -e "require('crypto').randomBytes(32).toString('hex')|console.log"
BANK_TOKEN_ENCRYPTION_KEY=
